#include "DemonWeapon.h"
//---------------------------------------
DemonWeapon::DemonWeapon()
{
	initWeapon();
}
//----------------------------------------
void DemonWeapon::initWeapon()
{
	//SpacielAttackTextureInfo temp = DataManager::instence().getSpacielAttackInfo("Demon");
	//if (temp.first != -1)
		//loadTextures(temp);
}